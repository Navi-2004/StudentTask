import axios from 'axios';

// Set up the default base URL for Axios
axios.defaults.baseURL = 'https://studentbackend-t5ld.onrender.com'; // Adjust the base URL as needed
// axios.defaults.baseURL = 'http://localhost:5000'; // Adjust the base URL as needed

export default axios;
